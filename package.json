{
  "name": "digitalco-inventory-sim",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/desk",
    "convex"
  ],
  "scripts": {
    "dev:desk": "npm run dev --workspace apps/desk",
    "build:desk": "npm run build --workspace apps/desk",
    "convex:dev": "npm run dev --workspace convex",
    "convex:deploy": "npm run deploy --workspace convex"
  }
}
